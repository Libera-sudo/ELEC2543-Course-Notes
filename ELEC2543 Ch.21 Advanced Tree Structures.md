#Y2S2 #ELEC2543
# Chapter 21. 高级树结构

`Pre: ` [[ELEC2543 Ch.20 Intro Trees]]

> [!abstract]
> 其他树结构 (*Other Tree Structures*) 把树的层级关系进一步用于搜索、存储、优先级访问与编码压缩。本章先介绍 AVL 树：它把二叉搜索树与平衡条件结合起来，使查找、插入与删除都尽量维持在 $O(\log_2 n)$；随后介绍堆 (*Heap*)：它是一种完全二叉树，并以根节点保存最小值的堆性质支持优先级访问；最后介绍编码树 (*Encoding Tree*)，尤其是 Huffman 编码树，说明如何依据字符频率构造平均码长尽量短的二进制编码。
>
> AVL 树的关键不是“形状看起来差不多”，而是任意节点左右子树的高度差不超过 $1$；一旦插入或删除破坏这一条件，就要通过单旋或双旋把失衡位置恢复。堆的关键不是二叉搜索树的大小关系，而是父节点不大于子节点的堆性质，因此根节点总是全树最小值，插入与删除最小值都只需沿一条路径调整。Huffman 编码则把“频率高的字符用短码、频率低的字符用长码”落到树结构上，通过反复合并最小频率子树得到前缀码。
>
> 本章先说明平衡树与 AVL 树的判定、旋转与插入删除；随后给出堆的定义、数组表示、插入、删除最小值与堆排序；最后介绍编码与 Huffman 树的构造、赋码与解码。

```table-of-contents
maxLevel: 3
```

## 21.1 平衡树与 AVL 树

平衡树 (*Balanced Tree*) 指左右子树高度尽量接近的二叉树。课件中对 AVL 树 (*Adelson-Velskii and Landis Tree*) 的要求更具体：对任意节点，它的左、右子树高度差最多只能是 $1$。如果把空树的高度定义为 $-1$，那么一个只有根节点的树高度就是 $0$。

AVL 树本质上是一棵自平衡二叉搜索树 (*Self-balancing Binary Search Tree*)。它仍然满足二叉搜索树的大小关系，只是额外加上了平衡约束，因此在最坏情况下，查找和插入也能维持在 $O(\log_2 n)$ 量级。

> [!note]
> AVL 树的平衡条件检查的是“高度差”，不是“节点数差”。只要某个节点左右子树的高度差超过 $1$，该节点就被视为失衡点，需要重新平衡。

#### AVL 树的性质

AVL 树在插入和删除时都遵循二叉搜索树的局部比较规则，因此新节点仍然要先落到正确的位置上；不同之处在于，操作完成后还要回溯检查路径上的祖先节点是否失衡。

- 若树保持平衡，查找、插入与删除都沿一条根到叶的路径进行，因此复杂度保持在 $O(\log_2 n)$。
- 若不做平衡修正，二叉搜索树可能退化成链表，最坏情况下复杂度会退化到 $O(n)$。
- AVL 树通过旋转把局部失衡修回去，因此不会长期积累成偏斜结构。

#### 插入后的重平衡

插入新节点时，先按二叉搜索树的顺序把它放到正确位置，再沿插入路径向上检查每个祖先节点。一旦某个节点的左右子树高度差变成 $2$，就必须在该失衡节点处旋转。

> [!comparison]
> 旋转的目标不是改变二叉搜索树的中序顺序，而是只调整局部父子关系，让树重新满足高度平衡。
>
> - 旋转前后，中序遍历的次序保持不变。
> - 变化的是节点之间的连接方式，而不是节点的相对大小顺序。

#### 单旋与双旋

失衡节点记为 $\alpha$。该节点是哪一种失衡，取决于哪一侧的“外孙树”太高。

- 若左孩子的左子树过高，则在 $\alpha$ 处做**单旋**，把左孩子提升为新的父节点。
- 若右孩子的右子树过高，则在 $\alpha$ 处做**单旋**，把右孩子提升为新的父节点。
- 若左孩子的右子树过高，则先对左孩子做一次旋转，再对 $\alpha$ 做一次旋转，这就是**双旋**。
- 若右孩子的左子树过高，则先对右孩子做一次旋转，再对 $\alpha$ 做一次旋转，这也是**双旋**。

```
左左失衡：单旋                     右右失衡：单旋

      a                                 a
     / \                               / \
    b   T4                            T1  b
   / \                                   / \
  T1 T2                                 T2 T4
 / \                                         / \
T3 T4                                       T1 T3

左右失衡：双旋                     右左失衡：双旋

      a                                 a
     / \                               / \
    b   T4                            T1  b
   / \                                   / \
  T1  c                                 c   T4
     / \                               / \
    T2 T3                             T2  T3
```

上面的四种情况只需要看“失衡方向”和“更深一层的偏斜方向”即可判断。单旋适用于“外侧”过高，双旋适用于“内侧”过高。

#### 删除后的重平衡

AVL 树删除节点时，先按二叉搜索树的规则定位要删的节点，再按节点孩子数量分别处理。

- 若待删节点只有一个孩子，则直接用该孩子顶替被删节点。
- 若待删节点有两个孩子，则用右子树中最左边的节点替换它，再继续删除那个替代节点。
- 删除完成后，同样要沿路径向上检查是否失衡，并在失衡点做旋转。

> [!example]- 示例：AVL 树的局部修正思路
>
> 课件用“先插入，后检查，再旋转”的方式展示 AVL 的工作流程。核心并不是记住每一个图形，而是记住处理顺序。
>
> 1. 先把新节点插到符合二叉搜索树规则的位置。
> 2. 回溯到第一个失衡祖先。
> 3. 判断是单旋还是双旋。
> 4. 旋转后继续向上检查，直到根节点。

## 21.2 堆

堆 (*Heap*) 是一种完全二叉树 (*Complete Binary Tree*)。完全二叉树的意思是：除最底层外，每一层都必须填满；如果最底层没有填满，节点必须尽量靠左排列。

课件中的堆是**最小堆** (*Min Heap*)：任一节点存储的值都小于或等于其孩子节点的值。这样一来，根节点一定保存全堆最小值。

> [!note]
> 堆不是二叉搜索树。它只保证“父节点不大于子节点”的局部性质，不保证左子树都小于右子树，也不保证中序遍历有序。

#### 堆性质与访问代价

因为最小值总在根节点，所以访问最小值只需要常数时间。删除最小值时，则要把最后一个节点移到根部，再沿着树向下调整回堆性质，因此删除操作需要 $O(\log_2 n)$ 时间。

#### 删除最小值

删除最小值时，课件采用的步骤是先把最后一个节点复制到根节点，再重新建立堆性质。

1. 取出根节点，也就是全堆最小值。
2. 用最后一个节点覆盖根节点。
3. 比较新根与它的两个孩子。
4. 若根比较大，就与较小的孩子交换。
5. 重复比较与交换，直到父节点不大于两个孩子为止。

```
删除最小值的重新堆化

         4                           15
      5     7       -->           5      7
    16 10  15                    16 10

把最后一个节点移到根部，再向下交换到正确位置
```

#### 插入新元素

插入新元素时，先把它放到最底层的最右侧空位，再向上调整。

1. 新元素先进入 bottommost level 的最右空位。
2. 将它与父节点比较。
3. 如果父节点更大，就交换。
4. 重复向上交换，直到父节点不大于新元素。

> [!example]- 示例：堆的插入与上浮
>
> 课件用 `5` 和 `4` 演示了插入后的上浮过程。新值先被放入底层末端，再一路向上和父节点比较，直到不再违反堆性质。
>
> 这说明堆的插入不是在树中“搜索位置”，而是在完全二叉树的最后一格先落位，再做局部修正。

#### 堆排序

堆排序 (*Heap Sort*) 的思路是先把所有元素逐个插入堆中，再不断删除最小值，直到堆为空。

- 插入每个元素都需要 $O(\log_2 n)$。
- 一共插入 $n$ 个元素，因此总插入代价是 $O(n \log_2 n)$。
- 删除每个元素也需要 $O(\log_2 n)$。
- 一共删除 $n$ 次，因此总删除代价也是 $O(n \log_2 n)$。
- 所以堆排序总复杂度为 $O(n \log_2 n)$。

#### 数组实现

堆可以用数组按层次顺序存储。若元素 `arr[i]` 是某个节点，则它的左右孩子与父节点可以直接由下标计算得到。

- 左孩子：`arr[2*i+1]`
- 右孩子：`arr[2*i+2]`
- 父节点：`arr[(i-1)/2]`

这种表示法让交换操作很方便，也能避免显式节点链接带来的额外管理成本。

```
数组表示的堆

           4
       5       7
    16  10   15

array: {4, 5, 7, 16, 10, 15}
```

## 21.3 编码树与 Huffman 编码

编码 (*Encoding*) 是用二进制串表示字符或符号的方式。编码树的目标不是让每个符号都占用固定长度，而是让整个文档所需的总比特数尽量少。

课件先给出固定长度编码的对照，再引入 Huffman 编码：如果某些字符出现得更频繁，就应该给它们更短的码；出现更少的字符则可以分配更长的码。这样得到的平均码长更小。

#### Huffman 编码树的构造

Huffman 编码树 (*Huffman Encoding Tree*) 的构造方法是重复合并频率最小的两个子树。

1. 一开始，每个字符各自形成一棵子树，根节点保存该字符的频率。
2. 把所有子树按频率从小到大排序。
3. 每次取出频率最小的两棵子树，把它们挂到一个新根节点下。
4. 新根的频率等于两棵子树频率之和。
5. 重复这个过程，直到所有子树连成一棵树。

> [!note]
> Huffman 树之所以有效，是因为频率高的字符会自然靠近根节点，从而得到较短的路径；频率低的字符则留在更深的位置，因此码长更长。

#### 码位分配与前缀码

树建好之后，再给边分配 `0` 和 `1`。课件强调，左边所有边要统一分配成同一个比特，右边所有边则分配另一个比特；如果同一方向在不同位置混用不同的比特，最终编码就会出错。

字符的编码就是从根节点走到该字符叶节点时经过的边序列。因为每个字符都在叶节点上，所以不会出现某个编码是另一个编码前缀的情况，这种性质叫作前缀码 (*Prefix Code*)。

#### 课件中的编码示例

课件用 `a, b, c, d, e, f` 的频率分别为 `1, 2, 3, 4, 5, 6` 来说明 Huffman 树的构造结果。对应的一组编码为：

- `a`: `1111`
- `b`: `1110`
- `c`: `110`
- `d`: `01`
- `e`: `00`
- `f`: `10`

在这组编码下，`fefecabdef` 可以编码为 `1000100011011111110010010`。

> [!example]- 示例：解码练习
>
> 按上面的码表，`0001110100111010` 的解码结果为 `edcfdcf`。
>
> - `00` 对应 `e`
> - `01` 对应 `d`
> - `110` 对应 `c`
> - `10` 对应 `f`
>
> 由于 Huffman 编码是前缀码，解码时只要从左到右沿树走到叶节点，再回到根节点继续读下一个比特即可。

#### 编码与解码的方向性

Huffman 编码的编码过程依赖树结构，解码过程也依赖同一棵树。

- 编码时，从根到叶得到字符对应的比特串。
- 解码时，从比特流出发，沿树边逐步行走，走到叶节点就输出一个字符。
- 因为是前缀码，所以解码时不会出现“某个码字与另一个码字冲突”的问题。

> [!note]
> Huffman 编码的核心不是“把树画出来”，而是“把频率信息转成路径长度”。频率越高，路径越短；频率越低，路径越长。
