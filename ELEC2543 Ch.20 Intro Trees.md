#Y2S2 #ELEC2543
# Chapter 20. 树

`Pre: ` [[ELEC2543 Ch.19 Data Structures]]
`Post:` [[ELEC2543 Ch.21 Advanced Tree Structures]]

> [!abstract]
> 树 (*Tree*) 是一种非线性数据结构，由节点 (*Node*) 与连接节点的边 (*Edge*) 组成；与线性结构相比，树结构把数据之间的关系组织成层级分叉的形式，因此更适合表达包含关系、层级关系与分治过程。对于任意两节点，树中只存在唯一一条连通路径，因此若一棵树共有 $N$ 个节点，就恰有 $N-1$ 条链接。
>
> Java 中的树通常用对象引用把节点串接起来，而不是像数组那样把元素平铺在连续位置上。二叉树 (*Binary Tree*) 是树的特例，每个节点最多只有两个孩子节点，分别称为左孩子与右孩子；这种结构既便于递归定义，也便于用递归方法实现遍历、插入与删除。
>
> 本章先说明树与二叉树的基本定义，再给出兄弟节点、叶节点、内部节点、子树、祖先、后代与树高等术语；随后介绍前序、中序与后序遍历，并通过示例说明不同遍历顺序的结果；最后引入有序二叉树 (*Ordered Binary Tree*) / 二叉搜索树 (*Binary Search Tree, BST*)，说明其插入与删除规则，并收束到 Java 中的递归实现思路。

```table-of-contents
maxLevel: 3
```

## 20.1 树与二叉树

树是一种非线性结构。它由若干节点以及连接这些节点的边组成，最核心的性质是任意两个节点之间只有唯一一条路径；因此，若一棵树有 $N$ 个节点，就一定有 $N-1$ 条链接。

一般树 (*General Tree*) 允许每个节点连接任意多个子节点，因此适合描述更一般的层级关系。

```
一般树

        A
      / | \
     B  C  D
        / \
       E   F
```

二叉树 (*Binary Tree*) 是树的一个特例。它在最顶层有一个根节点 (*Root*)，并且每个节点最多只有两个孩子：左孩子 (*Left Child*) 与右孩子 (*Right Child*)。

```
二叉树

        A
       / \
      B   C
     / \
    D   E
```

二叉树是本章后续遍历与有序树讨论的基础，因为左、右子树的划分使递归处理非常自然。

## 20.2 二叉树术语

二叉树中的若干术语彼此对应，理解它们有助于阅读后面的遍历与插入规则。

#### 兄弟节点、叶节点与内部节点

拥有同一个父节点的两个节点称为兄弟节点 (*Sibling*)。没有孩子节点的节点称为叶节点 (*Leaf*)；拥有一个或两个孩子节点的节点称为内部节点 (*Internal Node*)。

```
        parent(a)
           a
         /   \
        b     c

 b 和 c 是 siblings
 b 和 c 若都没有孩子，则是 leaves
 a 若有孩子，则是 internal node
```

#### 子树、祖先、后代与树高

某个节点及其所有后代组成它的子树 (*Subtree*)。位于从该节点通向根节点路径上的节点称为祖先 (*Ancestor*)；位于该节点子树中的节点称为后代 (*Descendant*)。

树高 (*Height*) 指从根节点到某个叶节点的最长路径所包含的边数。课件中的定义采用“边数”而不是“节点数”，因此单个根节点组成的树高为 $0$。

> [!note]
> 这里的树高按边数计量，和“路径上有多少个节点”不是同一个量；若把节点数算进去，结果会比树高多 $1$。

## 20.3 树遍历

遍历 (*Traversal*) 指按照某种固定顺序访问树中的每个节点。对于二叉树，课件给出了三种深度优先遍历 (*Depth First Traversal*)：前序、中序与后序。

> [!Comparison]
> 三种遍历的差别只在“根节点何时被访问”，而左右子树的访问顺序保持一致。
>
> **前序 (*Preorder*)**
> 先访问根，再遍历左子树，最后遍历右子树。
>
> **中序 (*Inorder*)**
> 先遍历左子树，再访问根，最后遍历右子树。
>
> **后序 (*Postorder*)**
> 先遍历左子树，再遍历右子树，最后访问根。

> [!example]- 示例：遍历课件中的二叉树
>
> 课件用同一棵树说明三种遍历顺序。对这棵树，三种访问序列分别如下。
>
> ```
>         10
>       /    \
>      6      15
>     / \    /
>    2   8  13
>     \     /
>      5   11
> ```
>
> 前序：`10, 6, 2, 5, 8, 15, 13, 11`
>
> 中序：`2, 5, 6, 8, 10, 11, 13, 15`
>
> 后序：`5, 2, 8, 6, 11, 13, 15, 10`
>
> **前序**
>
> - 先访问根节点 `10`，再进入左子树 `6`，最后进入右子树 `15`。
> - 左子树内部继续按同样规则展开，因此 `2`、`5`、`8` 的顺序由“根先于左右子树”决定。
>
> **中序**
>
> - 左子树、根、右子树的顺序会把节点按从小到大的顺序列出，当树满足有序二叉树性质时尤其明显。
> - 这也是后面判断二叉搜索树“是否有序”的关键遍历方式。
>
> **后序**
>
> - 先把左右子树完整访问完，再回到根节点。
> - 这种顺序很适合先处理子结构、再处理父结构的递归算法。

> [!question] 习题：写出遍历结果
> 请写出下图二叉树的中序、前序与后序遍历结果。
>
> ```
>     1
>    / \
>   2   3
>  / \
> 4   5
> ```
>
> > [!check]-
> > 按课件定义，三种遍历结果分别为：
> >
> > - 中序：`4, 2, 5, 1, 3`
> > - 前序：`1, 2, 4, 5, 3`
> > - 后序：`4, 5, 2, 3, 1`

## 20.4 有序二叉树

有序二叉树 (*Ordered Binary Tree*) 也叫二叉搜索树 (*Binary Search Tree, BST*)。它把对象放在适当的位置上，使中序遍历得到升序序列。

这意味着树中任一节点都要满足局部有序关系：左子树中的值不大于该节点，右子树中的值大于该节点。课件在插入规则里明确使用“`<=` 放左边、`>` 放右边”的约定，因此等值元素会被放到左子树。

```
        10
       /  \
      6    15
     / \   /
    2   8 13
     \     /
      5   11
```

有序二叉树的核心不是把数据“排好后再存进去”，而是每次插入时都要先搜索一个合适的位置。因为树本身已经维持了局部有序关系，所以后续查找、插入与删除都可以利用这一结构递归展开。

> [!note]
> 如果一棵二叉树的中序遍历不是升序，那么它就不满足二叉搜索树的性质；反过来，只要该性质成立，中序遍历就会把节点按大小顺序输出。

## 20.5 插入与删除

二叉搜索树的插入与删除都依赖“先定位，再修改”的思路。定位过程本身可以递归实现，因为每次比较后只会进入左子树或右子树中的一边。

#### 插入

插入规则如下：若根节点为空，则新元素直接成为根；若待插入值小于等于当前节点，则进入左子树；若待插入值更大，则进入右子树。

> [!example]- 示例：向有序二叉树中插入新值
>
> 课件按 `6、3、4、9、10` 的顺序演示插入。每一步都只向左或向右递归搜索，直到找到空位置。
>
> ```
> after add(6)
> 
>     6
> 
> after add(3)
> 
>     6
>    /
>   3
> 
> after add(4)
> 
>     6
>    /
>   3
>    \
>     4
> 
> after add(9)
> 
>     6
>    / \
>   3   9
>    \
>     4
> 
> after add(10)
> 
>     6
>    / \
>   3   9
>    \   \
>     4   10
> ```
>
> **根为空时直接建树**
>
> - 第一次插入 `6` 时，树为空，所以 `6` 直接成为根。
>
> **比较后只进入一侧**
>
> - `3` 小于 `6`，因此进入左子树并成为 `6` 的左孩子。
> - `4` 小于 `6`，继续进入左子树；再比较时 `4` 大于 `3`，因此成为 `3` 的右孩子。
>
> **递归插入的本质**
>
> - 每次只处理“当前节点以下的一棵更小的树”，所以插入过程天然适合递归写法。
> - 树的局部结构在每一步都保持有序，插入完成后无需整棵树重新排序。

#### 删除

删除节点时要先看节点本身的孩子数量，再决定如何调整树结构：

- 若目标节点是叶节点，直接删除即可。
- 若目标节点只有一个孩子，则把该孩子提升到原节点位置。
- 若目标节点有两个孩子，则用右子树中的最左节点替换它。

右子树中的最左节点也就是该子树中的最小值，课件在 Java 实现部分用 `findSmallestValue(Node root)` 递归寻找这一节点。

```
删除两孩子节点时的替换思路

        x                          successor
       / \              ->         /       \
      L   R                       L'        R
         /
   leftmost in R
```

> [!warning]
> 两个孩子都存在时不能简单把节点删掉，否则左子树和右子树会失去连接关系。必须先找到一个能同时维持有序性的替换值，再把原位置清理掉。

## 20.6 Java 实现

课件最后把树的基本操作落回 Java 递归实现。思路上通常先定义一个节点类，再让插入、删除与遍历方法围绕左、右引用递归展开。

树的插入可以递归地向左或向右下钻；删除则需要先处理叶节点、单孩子节点与双孩子节点三种情况。遍历方法则直接对应前序、中序与后序的访问顺序：访问根节点的位置不同，输出顺序也就不同。

`findSmallestValue(Node root)` 是删除操作中的辅助方法，它沿右子树不断向左走，直到找到最左边的节点；这个节点就是用来替换“有两个孩子的目标节点”的值。

---
`Pre: ` [[ELEC2543 Ch.19 Data Structures]]
`Post:` [[ELEC2543 Ch.21 Advanced Tree Structures]]
